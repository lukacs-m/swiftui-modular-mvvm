# Xcode
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/

# Generated by XcodeGen — run `xcodegen generate`
*.xcodeproj

# Swift Package Manager
.build/
.swiftpm/
# Note: Package.resolved is intentionally NOT ignored — committing it pins exact
# dependency versions so resolution is fast and deterministic for everyone.

# macOS
.DS_Store
